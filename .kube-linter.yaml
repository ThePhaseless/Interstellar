checks:
  exclude:
    # Third-party images don't support read-only root filesystems
    - "no-read-only-root-fs"
    # Many images require root (*arr apps, etc.)
    - "run-as-non-root"
    # Init containers and several workloads (e.g. Jellyfin) run without CPU/memory limits
    - "unset-cpu-requirements"
    - "unset-memory-requirements"
    # We intentionally use both ttlSecondsAfterFinished and history limits for defense-in-depth
    - "job-ttl-seconds-after-finished"
    # ServiceAccount in common/ kustomize base - linter can't resolve cross-references
    - "non-existent-service-account"
    # talos-kmsg-shipper needs privileged for /dev/kmsg (see its securityContext); justify any
    # other privileged pod there too, since this exclusion is global.
    - "privileged-container"
    - "privilege-escalation-container"

  include:
    - "dangling-ingress"
    - "dangling-horizontalpodautoscaler"
