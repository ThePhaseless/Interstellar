apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-loki-0-rwx
  namespace: observability
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-rwx
  resources:
    requests:
      storage: 1Gi
