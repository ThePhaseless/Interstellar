apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-mimir-0-rwx
  namespace: observability
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-rwx
  resources:
    requests:
      storage: 10Gi
