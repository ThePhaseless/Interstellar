apiVersion: 1
providers:
  - name: cluster
    folder: "Infrastructure"
    options:
      path: /var/lib/grafana/dashboards/cluster
  - name: traefik
    folder: "Infrastructure"
    options:
      path: /var/lib/grafana/dashboards/traefik
  - name: media
    folder: "Media"
    options:
      path: /var/lib/grafana/dashboards/media
  - name: logs
    folder: "Logs"
    options:
      path: /var/lib/grafana/dashboards/logs
  - name: geo
    folder: "Security"
    options:
      path: /var/lib/grafana/dashboards/geo
  - name: access
    folder: "Security"
    options:
      path: /var/lib/grafana/dashboards/access
  - name: longhorn
    folder: "Infrastructure"
    options:
      path: /var/lib/grafana/dashboards/longhorn
