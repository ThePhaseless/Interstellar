# =============================================================================
# NFS CSI Driver Bootstrap
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kube-system

resources:
  - storageclass.yaml

helmGlobals:
  chartHome: charts/csi-driver-nfs-v4.10.0

helmCharts:
  - name: csi-driver-nfs
    releaseName: csi-driver-nfs
    namespace: kube-system
    valuesInline:
      storageClass:
        create: false
