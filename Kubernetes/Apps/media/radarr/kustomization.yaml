apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml

# Placeholder secret for build-time validation
# Will be replaced by runtime-generated secret
secretGenerator:
  - name: radarr-api-key
    namespace: media
    literals:
      - api-key=placeholder
