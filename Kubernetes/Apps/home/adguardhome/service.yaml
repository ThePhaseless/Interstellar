apiVersion: v1
kind: Service
metadata:
  name: adguard-home
  namespace: home
spec:
  ports:
    - port: 84
      name: http
    - port: 53
      name: dns-tcp
    - port: 53
      name: dns-udp
      protocol: UDP
  selector:
    io.kompose.service: adguard-home
