apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - ingress.yaml
  - secrets.yaml

configMapGenerator:
  - files:
      - conf/config.yaml
    name: bazarr-config
    namespace: media
