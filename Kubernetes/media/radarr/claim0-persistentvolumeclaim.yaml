apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: radarr-claim0
  name: radarr-claim0
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
