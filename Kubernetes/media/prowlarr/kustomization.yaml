apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - ingress.yaml
  - api-extractor-job.yaml

configMapGenerator:
  - name: prowlarr-init-script
    namespace: media
    files:
      - scripts/configure-prowlarr.sh
