apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesFrom:
    - configMapKeyRef:
        name: traefik-values
        key: values.yaml
