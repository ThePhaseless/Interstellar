apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: homepage-claim0
  name: homepage-claim0
  namespace: utilities
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
