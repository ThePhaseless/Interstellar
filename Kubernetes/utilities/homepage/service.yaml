apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: homepage
  name: homepage
  namespace: utilities
spec:
  ports:
    - name: "82"
      port: 82
      targetPort: 3000
  selector:
    io.kompose.service: homepage
