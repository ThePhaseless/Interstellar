apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: homepage-data
  name: homepage-data
  namespace: utilities
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
