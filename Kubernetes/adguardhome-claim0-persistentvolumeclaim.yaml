apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: adguardhome-claim0
  name: adguardhome-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
