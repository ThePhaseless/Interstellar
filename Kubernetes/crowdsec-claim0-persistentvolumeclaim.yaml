apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: crowdsec-claim0
  name: crowdsec-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
