apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: authentik-server-claim1
  name: authentik-server-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
