apiVersion: traefik.io/v1alpha1
kind: IngressRouteUDP
metadata:
  name: adguard-home-dns-udp
  namespace: home
spec:
  entryPoints:
    - dns-udp
  routes:
    - services:
        - name: adguard-home
          port: 53
