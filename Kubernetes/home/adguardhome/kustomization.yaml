apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - ingressWeb.yaml
  - ingressDnsTcp.yaml
  - ingressDnsUdp.yaml

configMapGenerator:
  - name: adguard-home-config
    namespace: home
    files:
      - AdGuardHome.yaml=conf/AdGuardHome.yaml
