# =============================================================================
# Common Resources Kustomization
# =============================================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rbac.yaml
  - media-pv.yaml
  - externalsecrets.yaml

configMapGenerator:
  - name: arr-scripts
    namespace: media
    files:
      - scripts/extract-api-key.sh
      - scripts/configure-download-client.sh
      - scripts/configure-prowlarr.sh
      - scripts/configure-jellyseerr.sh
      - scripts/configure-jellyfin.sh
