# =============================================================================
# Common External Secrets
# =============================================================================
---
# API keys from Bitwarden Secrets Manager
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: arr-api-keys
  namespace: media
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-store
    kind: ClusterSecretStore
  target:
    name: arr-api-keys
    creationPolicy: Owner
    template:
      data:
        bazarr-api-key: ""
        jellyfin-api-key: ""
  data:
    - secretKey: sonarr-api-key
      remoteRef:
        key: sonarr-api-key
    - secretKey: radarr-api-key
      remoteRef:
        key: radarr-api-key
    - secretKey: prowlarr-api-key
      remoteRef:
        key: prowlarr-api-key
---
# Cloudflare API Token for DNS challenge
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cloudflare-api-token
  namespace: traefik
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: bitwarden-store
    kind: ClusterSecretStore
  target:
    name: cloudflare-api-token
    creationPolicy: Owner
  data:
    - secretKey: api-token
      remoteRef:
        key: cloudflare-api-token
