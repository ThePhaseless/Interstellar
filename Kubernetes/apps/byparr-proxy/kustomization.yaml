apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - deployment-1337x.yaml
  - deployment-eztv.yaml
  - service.yaml

configMapGenerator:
  - name: byparr-proxy-script
    files:
      - byparr-proxy.py
    options:
      disableNameSuffixHash: true
