apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: home

resources:
  - deployment.yaml
  - service.yaml
  - service-tailscale.yaml
  - ingress.yaml
  - pvc-v2.yaml
  - middleware.yaml

labels:
  - pairs:
      app: adguard
    includeSelectors: true
