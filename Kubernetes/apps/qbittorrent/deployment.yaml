apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbittorrent
  namespace: media
  labels:
    app: qbittorrent
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: qbittorrent
  template:
    metadata:
      labels:
        app: qbittorrent
    spec:
      initContainers:
        - name: config-init
          image: linuxserver/qbittorrent:5.1.4
          command:
            - /bin/sh
            - -c
            - |
              set -eu
              CONFIG_DIR="/config/qBittorrent"
              CONFIG_FILE="${CONFIG_DIR}/qBittorrent.conf"
              mkdir -p "${CONFIG_DIR}"
              if [ ! -f "${CONFIG_FILE}" ]; then
                cat > "${CONFIG_FILE}" <<'EOF'
              [Preferences]
              WebUI\Port=8080
              WebUI\AuthSubnetWhitelistEnabled=true
              WebUI\AuthSubnetWhitelist=0.0.0.0/0
              WebUI\CSRFProtection=true
              WebUI\HostHeaderValidation=true
              WebUI\ServerDomains=qbittorrent.nerine.dev,qbittorrent.media.svc.cluster.local,qbittorrent
              WebUI\LocalHostAuth=false
              WebUI\RootFolder=/vuetorrent
              EOF
                chmod 600 "${CONFIG_FILE}"
              fi
              # Credential auth is delegated to Traefik forward-auth, so the subnet
              # whitelist skips qBittorrent's own login. It stays 0.0.0.0/0 because
              # narrowing it to the pod CIDR made qBittorrent 401 every in-cluster
              # caller even though their IPs are inside 10.244.0.0/16; who can reach
              # port 8080 is already constrained by NetworkPolicy (Traefik + the
              # three *arr pods). CSRF and Host validation stay ON: the Authentik cookie
              # is scoped to nerine.dev, so without them any page on a sibling
              # subdomain could forge an authenticated setPreferences call, and
              # autorun_program turns that into command execution in this pod.
              # Spaces around "=" must match: configparser below rewrites with " = ".
              for KEY in CSRFProtection HostHeaderValidation ServerDomains LocalHostAuth AuthSubnetWhitelistEnabled AuthSubnetWhitelist; do
                sed -i "/^WebUI\\\\${KEY}[[:space:]]*=/d" "${CONFIG_FILE}"
              done
              sed -i '/^\[Preferences\]/a WebUI\\LocalHostAuth=false\nWebUI\\HostHeaderValidation=true\nWebUI\\ServerDomains=qbittorrent.nerine.dev,qbittorrent.media.svc.cluster.local,qbittorrent\nWebUI\\CSRFProtection=true\nWebUI\\AuthSubnetWhitelistEnabled=true\nWebUI\\AuthSubnetWhitelist=0.0.0.0\/0' "${CONFIG_FILE}"
              if ! grep -q '^\[BitTorrent\]' "${CONFIG_FILE}"; then
                printf '\n[BitTorrent]\nSession\\DefaultSavePath=/downloads\n' >> "${CONFIG_FILE}"
              elif ! grep -q 'DefaultSavePath' "${CONFIG_FILE}"; then
                sed -i '/^\[BitTorrent\]/a Session\\DefaultSavePath=/downloads' "${CONFIG_FILE}"
              else
                sed -i 's|Session\\DefaultSavePath=.*|Session\\DefaultSavePath=/downloads|' "${CONFIG_FILE}"
              fi

              # Reduce idle disk reads on the Storage pool by limiting active
              # seeding/checking torrents and stopping them after import.
              # Queueing must be enabled for MaxActive* limits to take effect.
              python3 - <<'PY'
              import configparser, os
              f = '/config/qBittorrent/qBittorrent.conf'
              cp = configparser.ConfigParser()
              cp.optionxform = str
              cp.read(f)
              if not cp.has_section('BitTorrent'):
                  cp.add_section('BitTorrent')
              updates = {
                  'Session\\QueueingSystemEnabled': 'true',
                  'Session\\GlobalMaxRatio': '2',
                  'Session\\GlobalMaxSeedingMinutes': '10080',
                  'Session\\ShareLimitAction': 'Stop',
              }
              changed = False
              for k, v in updates.items():
                  if cp.get('BitTorrent', k, fallback=None) != v:
                      cp.set('BitTorrent', k, v)
                      changed = True
              if changed:
                  with open(f, 'w') as fh:
                      cp.write(fh)
              PY
          volumeMounts:
            - name: config
              mountPath: /config
      containers:
        - name: qbittorrent
          image: linuxserver/qbittorrent:5.1.4
          envFrom:
            - configMapRef:
                name: media-runtime-config
          env:
            - name: TZ
              value: "UTC"
            - name: DOCKER_MODS
              value: ghcr.io/vuetorrent/vuetorrent-lsio-mod:latest@sha256:7cf3996f8c405a42d37c34c37d8eed7cbb01ef7c698c7a8ec243f53bd4b480cc
          ports:
            - name: http
              containerPort: 8080
            - name: bittorrent-tcp
              containerPort: 6881
            - name: bittorrent-udp
              containerPort: 6881
              protocol: UDP
          volumeMounts:
            - name: config
              mountPath: /config
            - name: downloads
              mountPath: /downloads
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 2000m
              memory: 2Gi
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10

      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: qbittorrent-config
        - name: downloads
          persistentVolumeClaim:
            claimName: downloads-pvc
