apiVersion: v1
kind: Service
metadata:
  name: copyparty
  namespace: utilities
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3923
      targetPort: http
  selector:
    app: copyparty

