# =============================================================================
# MCP Jungle Service
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  name: mcpjungle
  namespace: utilities
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app: mcpjungle
