# OpenTofu Linting Workflow
name: OpenTofu Lint

on:
  push:
    branches: [main]
    paths:
      - "OpenTofu/**"
      - ".github/workflows/opentofu-lint.yaml"
  pull_request:
    branches: [main]
    paths:
      - "OpenTofu/**"
      - ".github/workflows/opentofu-lint.yaml"

jobs:
  lint:
    name: Lint OpenTofu
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup TFLint
        uses: terraform-linters/setup-tflint@v4
        with:
          tflint_version: latest

      - name: Initialize TFLint
        run: tflint --init
        working-directory: OpenTofu

      - name: Run TFLint
        run: tflint --format=compact
        working-directory: OpenTofu
