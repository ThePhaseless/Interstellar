name: Apply Ansible Playbook on Oracle
on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Deploy Terraform Infrastructure"]
    types:
      - completed
  push:
    branches:
      - main
    paths:
      - "Ansible/**"
      - ".github/workflows/ansible-oracle.yaml"

jobs:
  call-ansible-workflow:
    uses: ./.github/workflows/ansible-template.yaml
    with:
      playbook-name: "site-oracle.yaml"
      workflow-name: "Apply Ansible Playbook on Oracle"
      concurrency-group: "ansible-oracle"
    secrets:
      GPG_SECRET: ${{ secrets.GPG_SECRET }}
      TS_OAUTH_CLIENT_ID: ${{ secrets.TS_OAUTH_CLIENT_ID }}
      TS_OAUTH_SECRET: ${{ secrets.TS_OAUTH_SECRET }}
      TS_PROXMOX_OAUTH_SECRET: ${{ secrets.TS_PROXMOX_OAUTH_SECRET }}
