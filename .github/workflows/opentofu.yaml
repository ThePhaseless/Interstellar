# =============================================================================
# OpenTofu CI/CD Workflow
# =============================================================================
# Runs OpenTofu plan on PRs and apply on main branch pushes.
# Uses Bitwarden Secrets Manager for all credentials.

name: OpenTofu

on:
  push:
    branches: [main]
    paths:
      - "OpenTofu/**"
      - ".github/workflows/opentofu.yaml"
  pull_request:
    branches: [main]
    paths:
      - "OpenTofu/**"
      - ".github/workflows/opentofu.yaml"

env:
  TOFU_VERSION: "1.9.0"
  TOFU_WORKING_DIR: "./OpenTofu"

jobs:
  # ---------------------------------------------------------------------------
  # OpenTofu Plan (runs on all PRs and pushes)
  # ---------------------------------------------------------------------------
  plan:
    name: Plan
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Fetch all secrets from Bitwarden (single project)
      - name: Get Secrets from Bitwarden
        uses: bitwarden/sm-action@v2
        with:
          access_token: ${{ secrets.BW_ACCESS_TOKEN }}
          secrets: |
            oci-config > OCI_CONFIG
            oci-private-key > OCI_PRIVATE_KEY
            oci-namespace > TF_VAR_oci_namespace
            tailscale-tailnet > TF_VAR_tailscale_tailnet
            tailscale-oauth-client-id > TS_OAUTH_CLIENT_ID
            tailscale-oauth-secret > TS_OAUTH_SECRET
            tf-state-bucket > TF_STATE_BUCKET
            cloudflare-api-token > CLOUDFLARE_API_TOKEN
            cloudflare-zone-id > TF_VAR_cloudflare_zone_id

      # Connect to Tailscale for Proxmox access
      - name: Connect to Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ env.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ env.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Write OCI config
        shell: bash
        run: |
          mkdir -p "$HOME/.oci"
          printf "%s" "$OCI_CONFIG" > "$HOME/.oci/config"
          printf "%s" "$OCI_PRIVATE_KEY" > "$HOME/.oci/oci_api_key.pem"
          chmod 600 "$HOME/.oci/config" "$HOME/.oci/oci_api_key.pem"

          if grep -q '^key_file=' "$HOME/.oci/config"; then
            sed -i "s|^key_file=.*|key_file=$HOME/.oci/oci_api_key.pem|" "$HOME/.oci/config"
          else
            echo "key_file=$HOME/.oci/oci_api_key.pem" >> "$HOME/.oci/config"
          fi

          tenancy=$(grep -E '^tenancy=' "$HOME/.oci/config" | head -n1 | cut -d= -f2-)
          if [[ -n "$tenancy" ]]; then
            echo "TF_VAR_oci_tenancy_ocid=$tenancy" >> "$GITHUB_ENV"
          fi

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: ${{ env.TOFU_VERSION }}

      - name: OpenTofu Format Check
        id: fmt
        run: tofu fmt -check -recursive
        working-directory: ${{ env.TOFU_WORKING_DIR }}
        continue-on-error: true

      - name: OpenTofu Init
        id: init
        run: |
          tofu init \
            -backend-config="bucket=${{ env.TF_STATE_BUCKET }}" \
            -backend-config="namespace=${{ env.TF_VAR_oci_namespace }}"
        working-directory: ${{ env.TOFU_WORKING_DIR }}
        env:
          BWS_ACCESS_TOKEN: ${{ secrets.BW_ACCESS_TOKEN }}

      - name: OpenTofu Validate
        id: validate
        run: tofu validate -no-color
        working-directory: ${{ env.TOFU_WORKING_DIR }}

      - name: OpenTofu Plan
        id: plan
        run: tofu plan -no-color -out=tfplan
        working-directory: ${{ env.TOFU_WORKING_DIR }}
        env:
          BWS_ACCESS_TOKEN: ${{ secrets.BW_ACCESS_TOKEN }}
        continue-on-error: true

      # Post plan output to PR
      - name: Update Pull Request
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request'
        env:
          PLAN: ${{ steps.plan.outputs.stdout }}
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `#### OpenTofu Format and Style 🖌\`${{ steps.fmt.outcome }}\`
            #### OpenTofu Initialization ⚙️\`${{ steps.init.outcome }}\`
            #### OpenTofu Validation 🤖\`${{ steps.validate.outcome }}\`
            #### OpenTofu Plan 📖\`${{ steps.plan.outcome }}\`

            <details><summary>Show Plan</summary>

            \`\`\`hcl
            ${process.env.PLAN}
            \`\`\`

            </details>

            *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: OpenTofu Plan Status
        if: steps.plan.outcome == 'failure'
        run: exit 1

      # Upload plan for apply job
      - name: Upload OpenTofu Plan
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: ${{ env.TOFU_WORKING_DIR }}/tfplan

  # ---------------------------------------------------------------------------
  # OpenTofu Apply (only on main branch)
  # ---------------------------------------------------------------------------
  apply:
    name: Apply
    runs-on: ubuntu-latest
    needs: plan
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment: server

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Fetch all secrets from Bitwarden (single project)
      - name: Get Secrets from Bitwarden
        uses: bitwarden/sm-action@v2
        with:
          access_token: ${{ secrets.BW_ACCESS_TOKEN }}
          secrets: |
            oci-config > OCI_CONFIG
            oci-private-key > OCI_PRIVATE_KEY
            oci-namespace > TF_VAR_oci_namespace
            tailscale-tailnet > TF_VAR_tailscale_tailnet
            tailscale-oauth-client-id > TS_OAUTH_CLIENT_ID
            tailscale-oauth-secret > TS_OAUTH_SECRET
            tf-state-bucket > TF_STATE_BUCKET
            cloudflare-api-token > CLOUDFLARE_API_TOKEN
            cloudflare-zone-id > TF_VAR_cloudflare_zone_id

      # Connect to Tailscale for Proxmox access
      - name: Connect to Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ env.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ env.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Write OCI config
        shell: bash
        run: |
          mkdir -p "$HOME/.oci"
          printf "%s" "$OCI_CONFIG" > "$HOME/.oci/config"
          printf "%s" "$OCI_PRIVATE_KEY" > "$HOME/.oci/oci_api_key.pem"
          chmod 600 "$HOME/.oci/config" "$HOME/.oci/oci_api_key.pem"

          if grep -q '^key_file=' "$HOME/.oci/config"; then
            sed -i "s|^key_file=.*|key_file=$HOME/.oci/oci_api_key.pem|" "$HOME/.oci/config"
          else
            echo "key_file=$HOME/.oci/oci_api_key.pem" >> "$HOME/.oci/config"
          fi

          tenancy=$(grep -E '^tenancy=' "$HOME/.oci/config" | head -n1 | cut -d= -f2-)
          if [[ -n "$tenancy" ]]; then
            echo "TF_VAR_oci_tenancy_ocid=$tenancy" >> "$GITHUB_ENV"
          fi

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: ${{ env.TOFU_VERSION }}

      - name: Download OpenTofu Plan
        uses: actions/download-artifact@v4
        with:
          name: tfplan
          path: ${{ env.TOFU_WORKING_DIR }}

      - name: OpenTofu Init
        run: |
          tofu init \
            -backend-config="bucket=${{ env.TF_STATE_BUCKET }}" \
            -backend-config="namespace=${{ env.TF_VAR_oci_namespace }}"
        working-directory: ${{ env.TOFU_WORKING_DIR }}
        env:
          BWS_ACCESS_TOKEN: ${{ secrets.BW_ACCESS_TOKEN }}

      - name: OpenTofu Apply
        run: tofu apply -auto-approve tfplan
        working-directory: ${{ env.TOFU_WORKING_DIR }}
        env:
          BWS_ACCESS_TOKEN: ${{ secrets.BW_ACCESS_TOKEN }}
